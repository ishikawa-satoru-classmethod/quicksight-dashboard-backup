# AWS Configuration
# AWS Account ID - auto-detected if not provided
# ACCOUNT_ID=123456789012

# AWS Profile Name - uses default credentials if not provided
# PROFILE_NAME=my-profile

# AWS Region - defaults to us-east-1
# REGION_NAME=us-east-1

# S3 Storage Configuration (optional)
# S3 bucket name for backup storage - uses local storage if not provided
# S3_BUCKET_NAME=my-quicksight-backups

# S3 key prefix for organizing backups - empty by default
# S3_KEY_PREFIX=production/dashboards

# Logging Configuration
# Log level: info or debug - defaults to info
# LOG_LEVEL=info

# Local Backup Configuration
# Local backup directory - defaults to 'backup'
# BACKUP_DIR=backup